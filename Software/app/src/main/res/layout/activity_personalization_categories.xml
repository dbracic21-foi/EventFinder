<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <TextView
        android:id="@+id/textViewTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/title_login"
        android:textSize="36sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="16dp"
        android:textColor="#2196F3"/>

    <!-- Linija ispod "Event Finder" -->
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_marginTop="70dp">
        <TextView
            android:id="@+id/textPersonalization"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/category_choose"
            android:textSize="26sp"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="16dp"
            android:textColor="#2196F3"/>
        <!-- Button 1: Zabavni -->
        <Button
            android:id="@+id/buttonZabavni"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:text="Zabavni"
            android:textSize="36sp"
            android:gravity="center"/>

        <!-- Button 2: Edukativni -->
        <Button
            android:id="@+id/buttonEdukativni"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:text="Edukativni"
            android:textSize="36sp"
            android:gravity="center"/>

        <!-- Button 3: Volonterski -->
        <Button
            android:id="@+id/buttonVolonterski"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:text="Volonterski"
            android:textSize="36sp"
            android:gravity="center"/>

    </LinearLayout>


</RelativeLayout>
